#!/usr/bin/env bash

version=0.0.1

source "${ROOT:-$PWD}/.bpan/run-or-docker.sh"

check() (
shopt -s inherit_errexit
  need bash 4.4
  need perl 5.28
  need perl YAML::PP::LibYAML
)

dockerfile() (
  from  alpine
  apk   yaml
  cpanm YAML::PP::LibYAML
)

run "$@"
