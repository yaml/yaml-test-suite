#!/usr/bin/env bash

version=0.0.1

source "${ROOT:-$PWD}/.bpan/run-or-docker.bash"

check() (
  need bash 4.4
  need perl 5.28
  need perl YAML::PP::LibYAML
)

dockerfile() (
  from alpine
  pkg  yaml
  cpan YAML::PP::LibYAML
)

run "$@"
