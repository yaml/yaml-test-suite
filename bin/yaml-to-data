#!/usr/bin/env bash

version=0.0.2

RUN_OR_DOCKER_PULL=true

source "${ROOT:-$PWD}/.bpan/run-or-docker.bash"

check() (
  need bash 4.4
  need perl 5.28
  need perl YAML::PP=0.028
)

dockerfile() (
  from alpine
  pkg  yaml
  cpan TINITA/YAML-PP-0.027_001.tar.gz
)

run "$@"
